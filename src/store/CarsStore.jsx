// import create from 'zustand';

// const useTCarsStore = create((set) => ({
//     cars: [],
//     getCars: (text) =>
//       set((state) => ({

//         todos: [...state.todos, { id: Date.now(), text, completed: false }],
//       })),
//   }));
  
//   export default useTodoStore;